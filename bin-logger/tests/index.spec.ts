// import { createLogger, LoggerOptions } from './../dist';

describe('test', () => {
  test('test1', () => {
    // const opt: LoggerOptions = {
    //   folderPath: __dirname,
    //   filename: 'test.log',
    //   level: 'debug',
    //   maxFileSize: 10,
    //   maxNumberFile: 10,
    // };
    // const logger = createLogger(opt);
    // for (let i = 0; i < 1000; i++)
    //   logger.info('hello==================================' + i, { error: new Error('test error' + i) });
    expect(1).toEqual(1);
  });
});
